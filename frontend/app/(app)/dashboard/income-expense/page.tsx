import { IncomeExpenseClient } from "@/components/IncomeExpenseClient";

export const metadata = {
  title: "Gelir/Gider — Cüzdan Koçu",
};

export default function DashboardIncomeExpensePage() {
  return <IncomeExpenseClient />;
}
